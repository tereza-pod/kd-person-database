class Helper {

def skipIfEmpty(input: String): Boolean = input.nonEmpty

def getConvoyDepartureLocation(input: Any): String = {   
    if (input.nonEmpty) {   
        input.split(" - ")(0) }
}

def getConvoyArrivalLocation(input: Any): String = {   
    if (input.nonEmpty) {
        input.split(" - ")(1) 
    }
}

}
